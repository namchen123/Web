
<nav class="navbar navbar-expand-lg navbar-light shadow">
    <div class="container d-flex justify-content-between align-items-center">

        <a class="navbar-brand text-success logo h1 align-self-center" href="index.html">
            Zay
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#templatemo_main_nav" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="align-self-center collapse navbar-collapse flex-fill  d-lg-flex justify-content-lg-between" id="templatemo_main_nav">
            <div class="flex-fill">
                <ul class="nav navbar-nav d-flex justify-content-between mx-lg-auto">
                    <li class="nav-item">
                        <a asp-controller="HomePage" asp-action="Index" class="nav-link" >Home</a>
                    </li>
                    <li class="nav-item">
                        <a asp-controller="HomePage" asp-action="About" class="nav-link">About</a>
                    </li>
                    <li class="nav-item">
                        <a asp-controller="HomePage" asp-action="Shop" class="nav-link">Shop</a>
                    </li>
                </ul>
            </div>
            <div class="navbar align-self-center d-flex">
                <div class="d-lg-none flex-sm-fill mt-3 mb-4 col-7 col-sm-auto pr-3">
                    <div class="input-group">
                        <input type="text" class="form-control" id="inputMobileSearch" placeholder="Search ...">
                        <div class="input-group-text">
                            <i class="fa fa-fw fa-search"></i>
                        </div>
                    </div>
                </div>
                <a class="nav-icon d-none d-lg-inline" href="#" data-bs-toggle="modal" data-bs-target="#templatemo_search">
                    <i class="fa fa-fw fa-search text-dark mr-2"></i>
                </a>
                <a asp-controller="Order" asp-action="OrderHistory" class="nav-icon d-none d-lg-inline">
                    <i class="fas fa-history text-dark mr-2"></i>
                </a>
                <a asp-controller="Order" asp-action="Cart" class="nav-icon position-relative text-decoration-none">
                    <i class="fa fa-fw fa-cart-arrow-down text-dark mr-1"></i>
                    <span class="position-absolute top-0 left-100 translate-middle badge rounded-pill bg-light text-dark">@ViewBag.Cart</span>
                </a>
                @if (Context.User.Identity.IsAuthenticated)
                {
                    if (Context.User.IsInRole("NHANVIEN"))
                    {
                        <a class="nav-link text-dark" asp-area="" asp-controller="Management" asp-action="SanPhamManagement">Quản trị web</a>
                    }
                    else 
                    {
                        <a asp-controller="Account" asp-action="GuestDetail" asp-route-user="@Context.User.Identity.Name" class="nav-link text-dark">Xin chào @Context.User.Identity.Name</a>
                    }
                    <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="Logout">Đăng Xuất</a>
                }   
                else
                {
                    <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="Login">Đăng nhập</a>
                    
                }
            </div>
        </div>

    </div>
</nav>
